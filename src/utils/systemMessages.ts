import { Logger } from "../classes/logger";
import { Messages } from "../classes/messages";

export const generalMessages = new Messages("general");
export const generalLogger = new Logger("System");
